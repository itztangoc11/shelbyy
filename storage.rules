rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Payment proofs are private and all uploads are performed by Netlify Functions.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
